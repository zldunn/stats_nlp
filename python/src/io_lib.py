'''
Reads in raw user input from the command line
Returns the users raw raw_input
Returns: String
'''
def read_cli_query():
    raw_in = input()
    return raw_in
import json

# def
# r = {'is_claimed': 'True', 'rating': 3.5}
# r = json.dumps(r)
# loaded_r = json.loads(r)
# loaded_r['rating'] #Output 3.5
# type(r) #Output str
# type(loaded_r) #Output dict
